// AUTO-GENERATED by scripts/generate-mysql-schema.mjs.
// Do not edit this file directly. Update db/schema.ts and regenerate instead.
// SQLite INTEGER maps to MySQL BIGINT (boolean modes map to BOOLEAN) to preserve
// source range and guarantee compatible foreign-key column types.
// Conditional unique constraints that MySQL cannot model as partial indexes
// are applied by database/mysql/003_partial_unique_constraints.sql.

import {
  bigint,
  boolean,
  check,
  customType,
  foreignKey,
  index,
  json,
  mysqlTable,
  primaryKey,
  text,
  uniqueIndex,
  varchar,
  type AnyMySqlColumn,
} from "drizzle-orm/mysql-core";

const longBlob = customType<{ data: Buffer; driverData: Buffer }>({
  dataType() {
    return "LONGBLOB";
  },
});
import { sql } from "drizzle-orm";

export const matrices = mysqlTable("matrices", {
  id: bigint("id", { mode: "number" }).primaryKey(),
  conventionId: bigint("convention_id", { mode: "number" }).notNull(),
  name: varchar("name", { length: 255 }).notNull(),
  status: varchar("status", { length: 80 }).notNull().default("ATIVO"),
});

export const branches = mysqlTable("branches", {
  id: bigint("id", { mode: "number" }).primaryKey(),
  matrixId: bigint("matrix_id", { mode: "number" })
    .notNull()
    .references(() => matrices.id),
  name: varchar("name", { length: 255 }).notNull(),
  status: varchar("status", { length: 80 }).notNull().default("ATIVO"),
});

export const users = mysqlTable("users", {
  id: bigint("id", { mode: "number" }).primaryKey(),
  conventionId: bigint("convention_id", { mode: "number" }).notNull(),
  name: varchar("name", { length: 255 }).notNull(),
  username: varchar("username", { length: 100 }).notNull().unique(),
  email: varchar("email", { length: 254 }).notNull().unique(),
  cpf: varchar("cpf", { length: 32 }).notNull().unique(),
  passwordSalt: varchar("password_salt", { length: 255 }).notNull(),
  passwordHash: varchar("password_hash", { length: 255 }).notNull(),
  roleName: varchar("role_name", { length: 255 }).notNull(),
  scope: varchar("scope", { length: 80 }).notNull(),
  boundMatrixId: bigint("bound_matrix_id", { mode: "number" }),
  boundBranchId: bigint("bound_branch_id", { mode: "number" }),
  status: varchar("status", { length: 80 }).notNull().default("ATIVO"),
  mustChangePassword: boolean("must_change_password")
    .notNull()
    .default(false),
  failedAttempts: bigint("failed_attempts", { mode: "number" }).notNull().default(0),
  blockedUntil: varchar("blocked_until", { length: 40 }),
  lastLoginAt: varchar("last_login_at", { length: 40 }),
  lastIdentifierType: varchar("last_identifier_type", { length: 80 }),
  lastOriginSummary: varchar("last_origin_summary", { length: 191 }),
});

export const sessions = mysqlTable("sessions", {
  id: varchar("id", { length: 191 }).primaryKey(),
  tokenHash: varchar("token_hash", { length: 191 }).notNull().unique(),
  userId: bigint("user_id", { mode: "number" })
    .notNull()
    .references(() => users.id),
  activeMatrixId: bigint("active_matrix_id", { mode: "number" }),
  activeBranchId: bigint("active_branch_id", { mode: "number" }),
  previousLoginAt: varchar("previous_login_at", { length: 40 }),
  previousIdentifierType: varchar("previous_identifier_type", { length: 80 }),
  previousOriginSummary: varchar("previous_origin_summary", { length: 191 }),
  expiresAt: varchar("expires_at", { length: 40 }).notNull(),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
  lastSeenAt: varchar("last_seen_at", { length: 40 }).notNull(),
});

export const auditLogs = mysqlTable("audit_logs", {
  id: bigint("id", { mode: "number" }).autoincrement().primaryKey(),
  userId: bigint("user_id", { mode: "number" }),
  tenantId: bigint("tenant_id", { mode: "number" }),
  event: varchar("event", { length: 191 }).notNull(),
  identifierType: varchar("identifier_type", { length: 80 }),
  reason: varchar("reason", { length: 191 }).notNull(),
  matrixId: bigint("matrix_id", { mode: "number" }),
  branchId: bigint("branch_id", { mode: "number" }),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
});

export const tenants = mysqlTable("tenants", {
  id: bigint("id", { mode: "number" }).primaryKey(),
  name: varchar("name", { length: 255 }).notNull(),
  slug: varchar("slug", { length: 191 }).notNull().unique(),
  accessCode: varchar("access_code", { length: 191 }).notNull().unique(),
  status: varchar("status", { length: 80 }).notNull().default("ATIVO"),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
  updatedAt: varchar("updated_at", { length: 40 }).notNull(),
});

// Auth foundation v2. The legacy tables above are intentionally retained so
// existing Site databases can receive this migration without destructive
// renames or data loss.
export const organizationalUnits = mysqlTable(
  "organizational_units",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    type: varchar("type", { length: 80 }).notNull(),
    name: varchar("name", { length: 255 }).notNull(),
    fantasyName: varchar("fantasy_name", { length: 255 }),
    legalName: varchar("legal_name", { length: 255 }),
    cnpj: varchar("cnpj", { length: 32 }),
    usesParentCnpj: boolean("uses_parent_cnpj")
      .notNull()
      .default(false),
    phone: varchar("phone", { length: 40 }),
    whatsapp: varchar("whatsapp", { length: 40 }),
    email: varchar("email", { length: 254 }),
    postalCode: varchar("postal_code", { length: 20 }),
    street: varchar("street", { length: 191 }),
    number: varchar("number", { length: 191 }),
    complement: varchar("complement", { length: 191 }),
    district: varchar("district", { length: 191 }),
    city: varchar("city", { length: 191 }),
    state: varchar("state", { length: 191 }),
    responsibleName: varchar("responsible_name", { length: 255 }),
    foundationDate: varchar("foundation_date", { length: 40 }),
    notes: text("notes"),
    code: varchar("code", { length: 191 }).notNull().unique(),
    parentId: bigint("parent_id", { mode: "number" }).references(
      (): AnyMySqlColumn => organizationalUnits.id,
    ),
    status: varchar("status", { length: 80 }).notNull().default("ATIVO"),
    archivedAt: varchar("archived_at", { length: 40 }),
    archivedBy: bigint("archived_by", { mode: "number" }),
    archivedPreviousStatus: varchar("archived_previous_status", { length: 80 }),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("organizational_units_id_tenant_unique").on(
      table.id,
      table.tenantId,
    ),
    uniqueIndex("organizational_units_hierarchy_scope_unique").on(
      table.id,
      table.parentId,
      table.tenantId,
    ),
  ],
);

export const organizationalFunctions = mysqlTable(
  "organizational_functions",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    name: varchar("name", { length: 255 }).notNull(),
    normalizedName: varchar("normalized_name", { length: 255 }).notNull(),
    description: text("description"),
    status: varchar("status", { length: 80 }).notNull().default("ATIVO"),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("organizational_functions_tenant_name_unique").on(
      table.tenantId,
      table.normalizedName,
    ),
    uniqueIndex("organizational_functions_id_tenant_unique").on(
      table.id,
      table.tenantId,
    ),
  ],
);

export const authUsers = mysqlTable(
  "auth_users",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).references(() => tenants.id),
    name: varchar("name", { length: 255 }).notNull(),
    username: varchar("username", { length: 100 }).notNull(),
    email: varchar("email", { length: 254 }).notNull(),
    cpf: varchar("cpf", { length: 32 }).notNull(),
    passwordHash: varchar("password_hash", { length: 255 }).notNull(),
    roleName: varchar("role_name", { length: 255 }).notNull(),
    scope: varchar("scope", { length: 80 }).notNull(),
    status: varchar("status", { length: 80 }).notNull().default("ATIVO"),
    mustChangePassword: boolean("must_change_password")
      .notNull()
      .default(false),
    failedAttempts: bigint("failed_attempts", { mode: "number" }).notNull().default(0),
    blockedUntil: varchar("blocked_until", { length: 40 }),
    archivedAt: varchar("archived_at", { length: 40 }),
    archivedBy: bigint("archived_by", { mode: "number" }),
    archivedPreviousStatus: varchar("archived_previous_status", { length: 80 }),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("auth_users_tenant_username_unique")
      .on(table.tenantId, table.username),
    uniqueIndex("auth_users_tenant_email_unique")
      .on(table.tenantId, table.email),
    uniqueIndex("auth_users_tenant_cpf_unique")
      .on(table.tenantId, table.cpf),
  ],
);

export const userUnitLinks = mysqlTable(
  "user_unit_links",
  {
    userId: bigint("user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    unitId: bigint("unit_id", { mode: "number" })
      .notNull()
      .references(() => organizationalUnits.id),
    isPrimary: boolean("is_primary")
      .notNull()
      .default(true),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
  (table) => [primaryKey({ columns: [table.userId, table.unitId] })],
);

// Organizational identities and credentials are tenant-owned. The nullable
// tenant is reserved exclusively for the singleton Platform Owner identity.
export const tenantMemberships = mysqlTable(
  "tenant_memberships",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    userId: bigint("user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    displayName: varchar("display_name", { length: 255 }).notNull(),
    roleName: varchar("role_name", { length: 255 }).notNull(),
    functionId: bigint("function_id", { mode: "number" }).references(
      () => organizationalFunctions.id,
    ),
    scope: varchar("scope", { length: 80 }).notNull(),
    scopeUnitId: bigint("scope_unit_id", { mode: "number" })
      .notNull()
      .references(() => organizationalUnits.id),
    status: varchar("status", { length: 80 }).notNull().default("ATIVO"),
    invitedByMembershipId: bigint("invited_by_membership_id", { mode: "number" }),
    acceptedAt: varchar("accepted_at", { length: 40 }),
    archivedAt: varchar("archived_at", { length: 40 }),
    archivedByMembershipId: bigint("archived_by_membership_id", { mode: "number" }),
    archivedPreviousStatus: varchar("archived_previous_status", { length: 80 }),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("tenant_memberships_user_tenant_unique").on(
      table.userId,
      table.tenantId,
    ),
  ],
);

export const membershipPermissions = mysqlTable(
  "membership_permissions",
  {
    membershipId: bigint("membership_id", { mode: "number" })
      .notNull()
      .references(() => tenantMemberships.id),
    permission: varchar("permission", { length: 191 }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
  (table) => [primaryKey({ columns: [table.membershipId, table.permission] })],
);

export const authSessions = mysqlTable("auth_sessions", {
  id: varchar("id", { length: 191 }).primaryKey(),
  tokenHash: varchar("token_hash", { length: 191 }).notNull().unique(),
  userId: bigint("user_id", { mode: "number" })
    .notNull()
    .references(() => authUsers.id),
  tenantId: bigint("tenant_id", { mode: "number" }).references(() => tenants.id),
  membershipId: bigint("membership_id", { mode: "number" }).references(() => tenantMemberships.id),
  organizationSelectionRequired: boolean("organization_selection_required")
    .notNull()
    .default(false),
  platformContextActive: boolean("platform_context_active")
    .notNull()
    .default(false),
  selectedUnitId: bigint("selected_unit_id", { mode: "number" }).references(
    () => organizationalUnits.id,
  ),
  previousLoginAt: varchar("previous_login_at", { length: 40 }),
  previousIdentifierType: varchar("previous_identifier_type", { length: 80 }),
  previousDeviceSummary: varchar("previous_device_summary", { length: 191 }),
  expiresAt: varchar("expires_at", { length: 40 }).notNull(),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
  lastSeenAt: varchar("last_seen_at", { length: 40 }).notNull(),
});

export const tenantAccessContexts = mysqlTable("tenant_access_contexts", {
  tokenHash: varchar("token_hash", { length: 191 }).primaryKey(),
  tenantId: bigint("tenant_id", { mode: "number" })
    .notNull()
    .references(() => tenants.id),
  expiresAt: varchar("expires_at", { length: 40 }).notNull(),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
  lastUsedAt: varchar("last_used_at", { length: 40 }).notNull(),
});

export const institutionLookupAttempts = mysqlTable(
  "institution_lookup_attempts",
  {
    id: bigint("id", { mode: "number" }).autoincrement().primaryKey(),
    codeFingerprint: varchar("code_fingerprint", { length: 191 }).notNull(),
    success: boolean("success").notNull(),
    ipAddress: varchar("ip_address", { length: 191 }),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
);

export const loginHistory = mysqlTable("login_history", {
  id: bigint("id", { mode: "number" }).autoincrement().primaryKey(),
  userId: bigint("user_id", { mode: "number" }).references(() => authUsers.id),
  tenantId: bigint("tenant_id", { mode: "number" }).references(() => tenants.id),
  identifierType: varchar("identifier_type", { length: 80 }).notNull(),
  identifierFingerprint: varchar("identifier_fingerprint", { length: 191 }).notNull(),
  success: boolean("success").notNull(),
  failureReason: varchar("failure_reason", { length: 191 }),
  ipAddress: varchar("ip_address", { length: 191 }),
  userAgent: text("user_agent"),
  deviceSummary: varchar("device_summary", { length: 191 }),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
});

export const userPermissions = mysqlTable(
  "user_permissions",
  {
    userId: bigint("user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    permission: varchar("permission", { length: 191 }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
  (table) => [primaryKey({ columns: [table.userId, table.permission] })],
);

export const administrationAudit = mysqlTable("administration_audit", {
  id: bigint("id", { mode: "number" }).autoincrement().primaryKey(),
  actorUserId: bigint("actor_user_id", { mode: "number" })
    .notNull()
    .references(() => authUsers.id),
  actorMembershipId: bigint("actor_membership_id", { mode: "number" }).references(
    () => tenantMemberships.id,
  ),
  tenantId: bigint("tenant_id", { mode: "number" })
    .notNull()
    .references(() => tenants.id),
  conventionId: bigint("convention_id", { mode: "number" })
    .notNull()
    .references(() => organizationalUnits.id),
  action: varchar("action", { length: 191 }).notNull(),
  entityType: varchar("entity_type", { length: 80 }).notNull(),
  entityId: bigint("entity_id", { mode: "number" }).notNull(),
  unitId: bigint("unit_id", { mode: "number" }).references(() => organizationalUnits.id),
  ipAddress: varchar("ip_address", { length: 191 }),
  userAgent: text("user_agent"),
  deviceSummary: varchar("device_summary", { length: 191 }),
  details: text("details"),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
});

export const unitLogos = mysqlTable("unit_logos", {
  unitId: bigint("unit_id", { mode: "number" })
    .primaryKey()
    .references(() => organizationalUnits.id),
  imageData: longBlob("image_data").notNull(),
  mimeType: varchar("mime_type", { length: 80 }).notNull(),
  byteSize: bigint("byte_size", { mode: "number" }).notNull(),
  updatedAt: varchar("updated_at", { length: 40 }).notNull(),
});

export const userProfilePhotos = mysqlTable("user_profile_photos", {
  userId: bigint("user_id", { mode: "number" })
    .primaryKey()
    .references(() => authUsers.id),
  imageData: longBlob("image_data").notNull(),
  mimeType: varchar("mime_type", { length: 80 }).notNull(),
  byteSize: bigint("byte_size", { mode: "number" }).notNull(),
  updatedAt: varchar("updated_at", { length: 40 }).notNull(),
});

// Platform ownership is deliberately isolated from user_permissions. The
// singleton constraint prevents a normal administrative flow from delegating
// or multiplying this platform-level authority.
export const platformOwners = mysqlTable(
  "platform_owners",
  {
    singletonId: bigint("singleton_id", { mode: "number" }).primaryKey().default(1),
    userId: bigint("user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    check("platform_owners_singleton_check", sql`${table.singletonId} = 1`),
    uniqueIndex("platform_owners_user_unique").on(table.userId),
  ],
);

// Unit references intentionally have no foreign keys so the audit trail
// survives the exceptional physical deletion of an empty/test unit.
export const platformAudit = mysqlTable("platform_audit", {
  id: bigint("id", { mode: "number" }).autoincrement().primaryKey(),
  actorUserId: bigint("actor_user_id", { mode: "number" })
    .notNull()
    .references(() => authUsers.id),
  tenantId: bigint("tenant_id", { mode: "number" }).references(() => tenants.id),
  action: varchar("action", { length: 191 }).notNull(),
  entityType: varchar("entity_type", { length: 80 }).notNull(),
  entityId: bigint("entity_id", { mode: "number" }).notNull(),
  conventionId: bigint("convention_id", { mode: "number" }),
  unitId: bigint("unit_id", { mode: "number" }),
  ipAddress: varchar("ip_address", { length: 191 }),
  userAgent: text("user_agent"),
  deviceSummary: varchar("device_summary", { length: 191 }),
  details: text("details"),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
});

// Commercial SaaS domain. These tables are deliberately separate from every
// future church cash-flow, tithe, offering, income or expense table.
export const commercialProfiles = mysqlTable("commercial_profiles", {
  tenantId: bigint("tenant_id", { mode: "number" })
    .primaryKey()
    .references(() => tenants.id),
  personType: varchar("person_type", { length: 80 }).notNull(),
  legalName: varchar("legal_name", { length: 255 }).notNull(),
  document: varchar("document", { length: 191 }),
  responsibleName: varchar("responsible_name", { length: 255 }),
  phone: varchar("phone", { length: 40 }),
  billingEmail: varchar("billing_email", { length: 254 }),
  notes: text("notes"),
  customerSince: varchar("customer_since", { length: 40 }).notNull(),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
  updatedAt: varchar("updated_at", { length: 40 }).notNull(),
});

export const saasPlans = mysqlTable(
  "saas_plans",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    name: varchar("name", { length: 255 }).notNull(),
    description: text("description"),
    priceCents: bigint("price_cents", { mode: "number" }).notNull(),
    billingPeriod: varchar("billing_period", { length: 191 }).notNull(),
    defaultGraceDays: bigint("default_grace_days", { mode: "number" }).notNull().default(5),
    defaultTrialDays: bigint("default_trial_days", { mode: "number" }).notNull().default(15),
    status: varchar("status", { length: 80 }).notNull().default("ATIVO"),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [uniqueIndex("saas_plans_name_unique").on(table.name)],
);

export const billingSettings = mysqlTable("billing_settings", {
  singletonId: bigint("singleton_id", { mode: "number" }).primaryKey().default(1),
  warningDays: bigint("warning_days", { mode: "number" }).notNull().default(7),
  pixKey: varchar("pix_key", { length: 191 }),
  pixKeyType: varchar("pix_key_type", { length: 80 }),
  payeeName: varchar("payee_name", { length: 255 }),
  bankName: varchar("bank_name", { length: 255 }),
  bankAgency: varchar("bank_agency", { length: 191 }),
  bankAccount: varchar("bank_account", { length: 191 }),
  instructions: text("instructions"),
  supportContact: varchar("support_contact", { length: 191 }),
  updatedAt: varchar("updated_at", { length: 40 }).notNull(),
});

export const tenantSubscriptions = mysqlTable("tenant_subscriptions", {
  id: bigint("id", { mode: "number" }).primaryKey(),
  tenantId: bigint("tenant_id", { mode: "number" })
    .notNull()
    .unique()
    .references(() => tenants.id),
  planId: bigint("plan_id", { mode: "number" }).references(() => saasPlans.id),
  contractedPriceCents: bigint("contracted_price_cents", { mode: "number" }).notNull().default(0),
  customPriceCents: bigint("custom_price_cents", { mode: "number" }),
  billingPeriod: varchar("billing_period", { length: 191 }).notNull(),
  status: varchar("status", { length: 80 }).notNull(),
  startDate: varchar("start_date", { length: 40 }).notNull(),
  nextDueDate: varchar("next_due_date", { length: 40 }),
  dueDay: bigint("due_day", { mode: "number" }),
  graceDays: bigint("grace_days", { mode: "number" }).notNull().default(5),
  trialStartDate: varchar("trial_start_date", { length: 40 }),
  trialEndDate: varchar("trial_end_date", { length: 40 }),
  accessUntil: varchar("access_until", { length: 40 }),
  autoRenew: boolean("auto_renew").notNull().default(true),
  notes: text("notes"),
  suspendedReason: varchar("suspended_reason", { length: 191 }),
  paymentProvider: varchar("payment_provider", { length: 191 }).notNull().default("MANUAL"),
  providerCustomerId: varchar("provider_customer_id", { length: 191 }),
  providerSubscriptionId: varchar("provider_subscription_id", { length: 191 }),
  externalReference: varchar("external_reference", { length: 191 }),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
  updatedAt: varchar("updated_at", { length: 40 }).notNull(),
});

export const saasCharges = mysqlTable(
  "saas_charges",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    subscriptionId: bigint("subscription_id", { mode: "number" })
      .notNull()
      .references(() => tenantSubscriptions.id),
    competence: varchar("competence", { length: 16 }).notNull(),
    description: text("description").notNull(),
    amountCents: bigint("amount_cents", { mode: "number" }).notNull(),
    issuedDate: varchar("issued_date", { length: 40 }).notNull(),
    dueDate: varchar("due_date", { length: 40 }).notNull(),
    status: varchar("status", { length: 80 }).notNull().default("PENDENTE"),
    paidAt: varchar("paid_at", { length: 40 }),
    paymentMethod: varchar("payment_method", { length: 191 }),
    notes: text("notes"),
    paymentProvider: varchar("payment_provider", { length: 191 }).notNull().default("MANUAL"),
    providerChargeId: varchar("provider_charge_id", { length: 191 }),
    externalReference: varchar("external_reference", { length: 191 }),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("saas_charges_subscription_due_unique").on(
      table.subscriptionId,
      table.dueDate,
    ),
  ],
);

export const saasPayments = mysqlTable("saas_payments", {
  id: bigint("id", { mode: "number" }).primaryKey(),
  tenantId: bigint("tenant_id", { mode: "number" })
    .notNull()
    .references(() => tenants.id),
  subscriptionId: bigint("subscription_id", { mode: "number" })
    .notNull()
    .references(() => tenantSubscriptions.id),
  chargeId: bigint("charge_id", { mode: "number" })
    .notNull()
    .unique()
    .references(() => saasCharges.id),
  amountCents: bigint("amount_cents", { mode: "number" }).notNull(),
  paidDate: varchar("paid_date", { length: 40 }).notNull(),
  paymentMethod: varchar("payment_method", { length: 191 }).notNull(),
  notes: text("notes"),
  paymentProvider: varchar("payment_provider", { length: 191 }).notNull().default("MANUAL"),
  providerPaymentId: varchar("provider_payment_id", { length: 191 }),
  externalReference: varchar("external_reference", { length: 191 }).unique(),
  createdBy: bigint("created_by", { mode: "number" })
    .notNull()
    .references(() => authUsers.id),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
});

export const commercialAudit = mysqlTable("commercial_audit", {
  id: bigint("id", { mode: "number" }).autoincrement().primaryKey(),
  actorUserId: bigint("actor_user_id", { mode: "number" })
    .notNull()
    .references(() => authUsers.id),
  tenantId: bigint("tenant_id", { mode: "number" })
    .notNull()
    .references(() => tenants.id),
  action: varchar("action", { length: 191 }).notNull(),
  entityType: varchar("entity_type", { length: 80 }).notNull(),
  entityId: bigint("entity_id", { mode: "number" }).notNull(),
  previousValues: text("previous_values"),
  newValues: text("new_values"),
  reason: varchar("reason", { length: 191 }),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
});

// Church people are intentionally independent from authentication identities.
export const memberSequences = mysqlTable("member_sequences", {
  tenantId: bigint("tenant_id", { mode: "number" })
    .primaryKey()
    .references(() => tenants.id),
  lastNumber: bigint("last_number", { mode: "number" }).notNull().default(0),
  updatedAt: varchar("updated_at", { length: 40 }).notNull(),
});

export const people = mysqlTable(
  "people",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    memberNumber: bigint("member_number", { mode: "number" }).notNull(),
    fullName: varchar("full_name", { length: 255 }).notNull(),
    status: varchar("status", { length: 80 }).notNull().default("MEMBRO_ATIVO"),
    birthDate: varchar("birth_date", { length: 40 }),
    sex: varchar("sex", { length: 191 }),
    cpf: varchar("cpf", { length: 32 }),
    rg: varchar("rg", { length: 191 }),
    voterTitle: varchar("voter_title", { length: 32 }),
    birthCity: varchar("birth_city", { length: 191 }),
    birthState: varchar("birth_state", { length: 191 }),
    phone: varchar("phone", { length: 40 }),
    whatsapp: varchar("whatsapp", { length: 40 }),
    email: varchar("email", { length: 254 }),
    motherName: varchar("mother_name", { length: 255 }),
    fatherName: varchar("father_name", { length: 255 }),
    maritalStatus: varchar("marital_status", { length: 80 }),
    spouseName: varchar("spouse_name", { length: 255 }),
    spousePersonId: bigint("spouse_person_id", { mode: "number" }),
    childrenCount: bigint("children_count", { mode: "number" }).notNull().default(0),
    postalCode: varchar("postal_code", { length: 20 }),
    street: varchar("street", { length: 191 }),
    addressNumber: varchar("address_number", { length: 191 }),
    complement: varchar("complement", { length: 191 }),
    district: varchar("district", { length: 191 }),
    city: varchar("city", { length: 191 }),
    state: varchar("state", { length: 191 }),
    profession: varchar("profession", { length: 191 }),
    workplace: varchar("workplace", { length: 191 }),
    educationLevel: varchar("education_level", { length: 191 }),
    theologicalEducation: text("theological_education"),
    primaryFunctionId: bigint("primary_function_id", { mode: "number" }),
    matrixId: bigint("matrix_id", { mode: "number" }).notNull(),
    branchId: bigint("branch_id", { mode: "number" }),
    churchEntryDate: varchar("church_entry_date", { length: 40 }),
    originChurch: varchar("origin_church", { length: 191 }),
    conversionDate: varchar("conversion_date", { length: 40 }),
    baptismDate: varchar("baptism_date", { length: 40 }),
    consecrationDate: varchar("consecration_date", { length: 40 }),
    notes: text("notes"),
    linkedAuthUserId: bigint("linked_auth_user_id", { mode: "number" }).references(
      () => authUsers.id,
    ),
    createdByUserId: bigint("created_by_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("people_tenant_member_number_unique").on(
      table.tenantId,
      table.memberNumber,
    ),
    uniqueIndex("people_tenant_cpf_unique")
      .on(table.tenantId, table.cpf),
    index("people_tenant_voter_title_idx").on(table.tenantId, table.voterTitle),
    uniqueIndex("people_id_tenant_unique").on(table.id, table.tenantId),
    foreignKey({
      columns: [table.spousePersonId, table.tenantId],
      foreignColumns: [table.id, table.tenantId],
      name: "people_spouse_tenant_fk",
    }),
    foreignKey({
      columns: [table.primaryFunctionId, table.tenantId],
      foreignColumns: [
        organizationalFunctions.id,
        organizationalFunctions.tenantId,
      ],
      name: "people_function_tenant_fk",
    }),
    foreignKey({
      columns: [table.matrixId, table.tenantId],
      foreignColumns: [organizationalUnits.id, organizationalUnits.tenantId],
      name: "people_matrix_tenant_fk",
    }),
    foreignKey({
      columns: [table.branchId, table.matrixId, table.tenantId],
      foreignColumns: [
        organizationalUnits.id,
        organizationalUnits.parentId,
        organizationalUnits.tenantId,
      ],
      name: "people_branch_matrix_tenant_fk",
    }),
  ],
);

export const personFunctions = mysqlTable(
  "person_functions",
  {
    personId: bigint("person_id", { mode: "number" }).notNull(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    functionId: bigint("function_id", { mode: "number" }).notNull(),
    isPrimary: boolean("is_primary")
      .notNull()
      .default(false),
    startedAt: varchar("started_at", { length: 40 }),
    endedAt: varchar("ended_at", { length: 40 }),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.personId, table.functionId] }),
    foreignKey({
      columns: [table.personId, table.tenantId],
      foreignColumns: [people.id, people.tenantId],
      name: "person_functions_person_tenant_fk",
    }),
    foreignKey({
      columns: [table.functionId, table.tenantId],
      foreignColumns: [
        organizationalFunctions.id,
        organizationalFunctions.tenantId,
      ],
      name: "person_functions_function_tenant_fk",
    }),
  ],
);

export const personHistory = mysqlTable(
  "person_history",
  {
    id: bigint("id", { mode: "number" }).autoincrement().primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    personId: bigint("person_id", { mode: "number" }).notNull(),
    eventType: varchar("event_type", { length: 80 }).notNull(),
    description: text("description").notNull(),
    eventDate: varchar("event_date", { length: 40 }),
    previousValues: text("previous_values"),
    newValues: text("new_values"),
    actorUserId: bigint("actor_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    actorMembershipId: bigint("actor_membership_id", { mode: "number" }).references(
      () => tenantMemberships.id,
    ),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
  (table) => [
    foreignKey({
      columns: [table.personId, table.tenantId],
      foreignColumns: [people.id, people.tenantId],
      name: "person_history_person_tenant_fk",
    }),
  ],
);

export const personRelationships = mysqlTable(
  "person_relationships",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    personId: bigint("person_id", { mode: "number" }).notNull(),
    relatedPersonId: bigint("related_person_id", { mode: "number" }).notNull(),
    relationshipType: varchar("relationship_type", { length: 80 }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("person_relationships_unique").on(
      table.personId,
      table.relatedPersonId,
      table.relationshipType,
    ),
    foreignKey({
      columns: [table.personId, table.tenantId],
      foreignColumns: [people.id, people.tenantId],
      name: "person_relationships_person_tenant_fk",
    }),
    foreignKey({
      columns: [table.relatedPersonId, table.tenantId],
      foreignColumns: [people.id, people.tenantId],
      name: "person_relationships_related_tenant_fk",
    }),
  ],
);

export const memberPhotos = mysqlTable(
  "member_photos",
  {
    personId: bigint("person_id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    imageData: longBlob("image_data").notNull(),
    mimeType: varchar("mime_type", { length: 80 }).notNull(),
    byteSize: bigint("byte_size", { mode: "number" }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    foreignKey({
      columns: [table.personId, table.tenantId],
      foreignColumns: [people.id, people.tenantId],
      name: "member_photos_person_tenant_fk",
    }),
  ],
);

export const memberCustomFields = mysqlTable(
  "member_custom_fields",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    name: varchar("name", { length: 255 }).notNull(),
    normalizedName: varchar("normalized_name", { length: 255 }).notNull(),
    fieldType: varchar("field_type", { length: 80 }).notNull(),
    helpText: text("help_text"),
    required: boolean("required").notNull().default(false),
    status: varchar("status", { length: 80 }).notNull().default("ATIVO"),
    displayOrder: bigint("display_order", { mode: "number" }).notNull().default(0),
    sectionName: varchar("section_name", { length: 255 })
      .notNull()
      .default("Informações adicionais"),
    showAdmin: boolean("show_admin")
      .notNull()
      .default(true),
    showPublic: boolean("show_public")
      .notNull()
      .default(false),
    showPrint: boolean("show_print")
      .notNull()
      .default(false),
    optionsJson: text("options_json"),
    createdByUserId: bigint("created_by_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("member_custom_fields_tenant_name_unique").on(
      table.tenantId,
      table.normalizedName,
    ),
    uniqueIndex("member_custom_fields_id_tenant_unique").on(
      table.id,
      table.tenantId,
    ),
    index("member_custom_fields_tenant_status_order_idx").on(
      table.tenantId,
      table.status,
      table.displayOrder,
    ),
  ],
);

export const memberCustomValues = mysqlTable(
  "member_custom_values",
  {
    personId: bigint("person_id", { mode: "number" }).notNull(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    fieldId: bigint("field_id", { mode: "number" }).notNull(),
    valueText: varchar("value_text", { length: 191 }).notNull(),
    updatedByUserId: bigint("updated_by_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.personId, table.fieldId] }),
    foreignKey({
      columns: [table.personId, table.tenantId],
      foreignColumns: [people.id, people.tenantId],
      name: "member_custom_values_person_tenant_fk",
    }),
    foreignKey({
      columns: [table.fieldId, table.tenantId],
      foreignColumns: [memberCustomFields.id, memberCustomFields.tenantId],
      name: "member_custom_values_field_tenant_fk",
    }),
  ],
);

export const memberPreRegistrationForms = mysqlTable(
  "member_pre_registration_forms",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    name: varchar("name", { length: 255 }).notNull(),
    tokenHash: varchar("token_hash", { length: 191 }).notNull().unique(),
    tokenPrefix: varchar("token_prefix", { length: 191 }).notNull(),
    unitId: bigint("unit_id", { mode: "number" }),
    status: varchar("status", { length: 80 }).notNull().default("ATIVO"),
    expiresAt: varchar("expires_at", { length: 40 }),
    createdByUserId: bigint("created_by_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("member_pre_registration_forms_id_tenant_unique").on(
      table.id,
      table.tenantId,
    ),
    index("member_pre_registration_forms_tenant_status_idx").on(
      table.tenantId,
      table.status,
      table.createdAt,
    ),
    foreignKey({
      columns: [table.unitId, table.tenantId],
      foreignColumns: [organizationalUnits.id, organizationalUnits.tenantId],
      name: "pre_registration_forms_unit_tenant_fk",
    }),
  ],
);

export const memberPreRegistrations = mysqlTable(
  "member_pre_registrations",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    formId: bigint("form_id", { mode: "number" }).notNull(),
    fullName: varchar("full_name", { length: 255 }).notNull(),
    birthDate: varchar("birth_date", { length: 40 }),
    cpf: varchar("cpf", { length: 32 }),
    phone: varchar("phone", { length: 40 }),
    whatsapp: varchar("whatsapp", { length: 40 }),
    email: varchar("email", { length: 254 }),
    voterTitle: varchar("voter_title", { length: 32 }),
    matrixId: bigint("matrix_id", { mode: "number" }),
    branchId: bigint("branch_id", { mode: "number" }),
    status: varchar("status", { length: 80 }).notNull().default("PENDENTE"),
    payloadJson: text("payload_json").notNull(),
    consentAt: varchar("consent_at", { length: 40 }).notNull(),
    consentVersion: varchar("consent_version", { length: 191 }).notNull(),
    sourceHash: varchar("source_hash", { length: 191 }).notNull(),
    reviewReason: varchar("review_reason", { length: 191 }),
    reviewedByUserId: bigint("reviewed_by_user_id", { mode: "number" }).references(
      () => authUsers.id,
    ),
    reviewedAt: varchar("reviewed_at", { length: 40 }),
    approvedMemberId: bigint("approved_member_id", { mode: "number" }),
    correctionTokenHash: varchar("correction_token_hash", { length: 191 }),
    correctionExpiresAt: varchar("correction_expires_at", { length: 40 }),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("member_pre_registrations_id_tenant_unique").on(
      table.id,
      table.tenantId,
    ),
    index("member_pre_registrations_tenant_status_created_idx").on(
      table.tenantId,
      table.status,
      table.createdAt,
    ),
    index("member_pre_registrations_tenant_cpf_idx").on(
      table.tenantId,
      table.cpf,
    ),
    foreignKey({
      columns: [table.formId, table.tenantId],
      foreignColumns: [
        memberPreRegistrationForms.id,
        memberPreRegistrationForms.tenantId,
      ],
      name: "pre_registrations_form_tenant_fk",
    }),
    foreignKey({
      columns: [table.matrixId, table.tenantId],
      foreignColumns: [organizationalUnits.id, organizationalUnits.tenantId],
      name: "pre_registrations_matrix_tenant_fk",
    }),
    foreignKey({
      columns: [table.branchId, table.matrixId, table.tenantId],
      foreignColumns: [
        organizationalUnits.id,
        organizationalUnits.parentId,
        organizationalUnits.tenantId,
      ],
      name: "pre_registrations_branch_matrix_tenant_fk",
    }),
    foreignKey({
      columns: [table.approvedMemberId, table.tenantId],
      foreignColumns: [people.id, people.tenantId],
      name: "pre_registrations_member_tenant_fk",
    }),
  ],
);

export const memberPreRegistrationPhotos = mysqlTable(
  "member_pre_registration_photos",
  {
    preRegistrationId: bigint("pre_registration_id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    imageData: longBlob("image_data").notNull(),
    mimeType: varchar("mime_type", { length: 80 }).notNull(),
    byteSize: bigint("byte_size", { mode: "number" }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    foreignKey({
      columns: [table.preRegistrationId, table.tenantId],
      foreignColumns: [
        memberPreRegistrations.id,
        memberPreRegistrations.tenantId,
      ],
      name: "pre_registration_photos_request_tenant_fk",
    }),
  ],
);

export const memberPreRegistrationCustomValues = mysqlTable(
  "member_pre_registration_custom_values",
  {
    preRegistrationId: bigint("pre_registration_id", { mode: "number" }).notNull(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    fieldId: bigint("field_id", { mode: "number" }).notNull(),
    valueText: varchar("value_text", { length: 191 }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.preRegistrationId, table.fieldId] }),
    foreignKey({
      columns: [table.preRegistrationId, table.tenantId],
      foreignColumns: [
        memberPreRegistrations.id,
        memberPreRegistrations.tenantId,
      ],
      name: "pre_registration_values_request_tenant_fk",
    }),
    foreignKey({
      columns: [table.fieldId, table.tenantId],
      foreignColumns: [memberCustomFields.id, memberCustomFields.tenantId],
      name: "pre_registration_values_field_tenant_fk",
    }),
  ],
);

export const memberPreRegistrationRateLimits = mysqlTable(
  "member_pre_registration_rate_limits",
  {
    rateKey: varchar("rate_key", { length: 191 }).primaryKey(),
    attempts: bigint("attempts", { mode: "number" }).notNull().default(0),
    windowStartedAt: varchar("window_started_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
);

export const helpArticles = mysqlTable(
  "help_articles",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).references(() => tenants.id),
    slug: varchar("slug", { length: 191 }).notNull(),
    title: varchar("title", { length: 255 }).notNull(),
    summary: text("summary").notNull(),
    content: text("content").notNull(),
    category: varchar("category", { length: 191 }).notNull(),
    displayOrder: bigint("display_order", { mode: "number" }).notNull().default(0),
    targetProfiles: text("target_profiles").notNull().default('["TODOS"]'),
    requiredPermission: varchar("required_permission", { length: 191 }),
    relatedRoute: varchar("related_route", { length: 191 }),
    published: boolean("published")
      .notNull()
      .default(true),
    isNewFeature: boolean("is_new_feature")
      .notNull()
      .default(false),
    releasedAt: varchar("released_at", { length: 40 }),
    version: varchar("version", { length: 191 }).notNull().default("1.0"),
    createdByUserId: bigint("created_by_user_id", { mode: "number" }).references(
      () => authUsers.id,
    ),
    publishedAt: varchar("published_at", { length: 40 }),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("help_articles_visibility_idx").on(
      table.published,
      table.category,
      table.displayOrder,
      table.releasedAt,
    ),
  ],
);

export const helpArticleReads = mysqlTable(
  "help_article_reads",
  {
    userId: bigint("user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    articleId: bigint("article_id", { mode: "number" })
      .notNull()
      .references(() => helpArticles.id),
    viewedAt: varchar("viewed_at", { length: 40 }).notNull(),
  },
  (table) => [primaryKey({ columns: [table.userId, table.articleId] })],
);

export const dataExportAudit = mysqlTable(
  "data_export_audit",
  {
    id: bigint("id", { mode: "number" }).autoincrement().primaryKey(),
    actorUserId: bigint("actor_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    actorMembershipId: bigint("actor_membership_id", { mode: "number" }).references(
      () => tenantMemberships.id,
    ),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    exportType: varchar("export_type", { length: 80 }).notNull(),
    modules: text("modules").notNull(),
    format: varchar("format", { length: 191 }).notNull(),
    recordCount: bigint("record_count", { mode: "number" }).notNull().default(0),
    scopeUnitId: bigint("scope_unit_id", { mode: "number" }).references(
      () => organizationalUnits.id,
    ),
    status: varchar("status", { length: 80 }).notNull(),
    details: text("details"),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("data_export_audit_tenant_created_idx").on(
      table.tenantId,
      table.createdAt,
    ),
  ],
);

export const notifications = mysqlTable(
  "notifications",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).references(() => tenants.id),
    audience: varchar("audience", { length: 191 }).notNull().default("ORGANIZATIONAL"),
    type: varchar("type", { length: 80 }).notNull(),
    title: varchar("title", { length: 255 }).notNull(),
    message: text("message").notNull(),
    priority: varchar("priority", { length: 191 }).notNull().default("INFO"),
    internalRoute: varchar("internal_route", { length: 191 }),
    sourceEntity: varchar("source_entity", { length: 191 }),
    sourceEntityId: bigint("source_entity_id", { mode: "number" }),
    unitId: bigint("unit_id", { mode: "number" }).references(() => organizationalUnits.id),
    groupKey: varchar("group_key", { length: 191 }),
    metadataJson: text("metadata_json"),
    mandatory: boolean("mandatory")
      .notNull()
      .default(false),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("notifications_tenant_type_created_idx").on(
      table.tenantId,
      table.type,
      table.createdAt,
    ),
    index("notifications_group_key_idx").on(table.groupKey, table.createdAt),
  ],
);

export const notificationRecipients = mysqlTable(
  "notification_recipients",
  {
    notificationId: bigint("notification_id", { mode: "number" })
      .notNull()
      .references(() => notifications.id),
    userId: bigint("user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    readAt: varchar("read_at", { length: 40 }),
    archivedAt: varchar("archived_at", { length: 40 }),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.notificationId, table.userId] }),
    index("notification_recipients_user_unread_idx").on(
      table.userId,
      table.readAt,
      table.notificationId,
    ),
    index("notification_recipients_user_created_idx").on(
      table.userId,
      table.createdAt,
    ),
  ],
);

export const departments = mysqlTable(
  "departments",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" })
      .notNull()
      .references(() => tenants.id),
    name: varchar("name", { length: 255 }).notNull(),
    acronym: varchar("acronym", { length: 191 }),
    description: text("description"),
    type: varchar("type", { length: 80 }).notNull(),
    unitId: bigint("unit_id", { mode: "number" })
      .notNull()
      .references(() => organizationalUnits.id),
    conventionId: bigint("convention_id", { mode: "number" })
      .notNull()
      .references(() => organizationalUnits.id),
    matrixId: bigint("matrix_id", { mode: "number" }).references(() => organizationalUnits.id),
    branchId: bigint("branch_id", { mode: "number" }).references(() => organizationalUnits.id),
    status: varchar("status", { length: 80 }).notNull().default("ATIVO"),
    enabledFeatures: text("enabled_features").notNull(),
    absenceAlertThreshold: bigint("absence_alert_threshold", { mode: "number" })
      .notNull()
      .default(3),
    version: bigint("version", { mode: "number" }).notNull().default(1),
    createdByUserId: bigint("created_by_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("departments_id_tenant_unique").on(table.id, table.tenantId),
    uniqueIndex("departments_tenant_unit_name_unique").on(
      table.tenantId,
      table.unitId,
      table.name,
    ),
    index("departments_tenant_scope_status_idx").on(
      table.tenantId,
      table.conventionId,
      table.matrixId,
      table.branchId,
      table.status,
    ),
  ],
);
export const departmentRoles = mysqlTable(
  "department_roles",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    departmentId: bigint("department_id", { mode: "number" })
      .notNull()
      .references(() => departments.id),
    name: varchar("name", { length: 255 }).notNull(),
    isLeadership: boolean("is_leadership")
      .notNull()
      .default(false),
    displayOrder: bigint("display_order", { mode: "number" }).notNull().default(0),
    status: varchar("status", { length: 80 }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("department_roles_tenant_department_name_unique").on(
      table.tenantId,
      table.departmentId,
      table.name,
    ),
  ],
);
export const departmentParticipants = mysqlTable(
  "department_participants",
  {
    departmentId: bigint("department_id", { mode: "number" })
      .notNull()
      .references(() => departments.id),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    personId: bigint("person_id", { mode: "number" })
      .notNull()
      .references(() => people.id),
    roleId: bigint("role_id", { mode: "number" }).references(() => departmentRoles.id),
    joinedAt: varchar("joined_at", { length: 40 }).notNull(),
    status: varchar("status", { length: 80 }).notNull(),
    leftAt: varchar("left_at", { length: 40 }),
    exitReason: varchar("exit_reason", { length: 191 }),
    notes: text("notes"),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.departmentId, table.personId] }),
    index("department_participants_tenant_person_idx").on(
      table.tenantId,
      table.personId,
      table.status,
    ),
  ],
);
export const departmentAccess = mysqlTable(
  "department_access",
  {
    departmentId: bigint("department_id", { mode: "number" })
      .notNull()
      .references(() => departments.id),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    membershipId: bigint("membership_id", { mode: "number" })
      .notNull()
      .references(() => tenantMemberships.id),
    roleId: bigint("role_id", { mode: "number" }).references(() => departmentRoles.id),
    permissionsJson: text("permissions_json").notNull(),
    status: varchar("status", { length: 80 }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.departmentId, table.membershipId] }),
    index("department_access_membership_idx").on(
      table.tenantId,
      table.membershipId,
      table.status,
    ),
  ],
);
export const departmentEvents = mysqlTable(
  "department_events",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    departmentId: bigint("department_id", { mode: "number" })
      .notNull()
      .references(() => departments.id),
    title: varchar("title", { length: 255 }).notNull(),
    description: text("description"),
    eventDate: varchar("event_date", { length: 40 }).notNull(),
    startTime: varchar("start_time", { length: 40 }),
    location: varchar("location", { length: 191 }),
    responsiblePersonId: bigint("responsible_person_id", { mode: "number" }).references(
      () => people.id,
    ),
    notes: text("notes"),
    status: varchar("status", { length: 80 }).notNull(),
    createdByUserId: bigint("created_by_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("department_events_department_date_idx").on(
      table.tenantId,
      table.departmentId,
      table.eventDate,
      table.status,
    ),
  ],
);
export const departmentActivities = mysqlTable("department_activities", {
  id: bigint("id", { mode: "number" }).primaryKey(),
  tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
  departmentId: bigint("department_id", { mode: "number" })
    .notNull()
    .references(() => departments.id),
  activityDate: varchar("activity_date", { length: 40 }).notNull(),
  title: varchar("title", { length: 255 }).notNull(),
  notes: text("notes"),
  status: varchar("status", { length: 80 }).notNull(),
  version: bigint("version", { mode: "number" }).notNull(),
  finalizedByUserId: bigint("finalized_by_user_id", { mode: "number" }).references(
    () => authUsers.id,
  ),
  finalizedAt: varchar("finalized_at", { length: 40 }),
  createdByUserId: bigint("created_by_user_id", { mode: "number" })
    .notNull()
    .references(() => authUsers.id),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
  updatedAt: varchar("updated_at", { length: 40 }).notNull(),
});
export const departmentAttendance = mysqlTable(
  "department_attendance",
  {
    activityId: bigint("activity_id", { mode: "number" })
      .notNull()
      .references(() => departmentActivities.id),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    departmentId: bigint("department_id", { mode: "number" })
      .notNull()
      .references(() => departments.id),
    personId: bigint("person_id", { mode: "number" })
      .notNull()
      .references(() => people.id),
    attendanceStatus: varchar("attendance_status", { length: 80 }).notNull(),
    notes: text("notes"),
    updatedByUserId: bigint("updated_by_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.activityId, table.personId] }),
    index("department_attendance_person_idx").on(
      table.tenantId,
      table.personId,
      table.attendanceStatus,
    ),
  ],
);
export const departmentCommunications = mysqlTable(
  "department_communications",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    departmentId: bigint("department_id", { mode: "number" })
      .notNull()
      .references(() => departments.id),
    message: text("message").notNull(),
    audience: varchar("audience", { length: 191 }).notNull(),
    channel: varchar("channel", { length: 191 }).notNull(),
    recipientCount: bigint("recipient_count", { mode: "number" }).notNull(),
    createdByUserId: bigint("created_by_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
);
export const ebdClasses = mysqlTable(
  "ebd_classes",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    departmentId: bigint("department_id", { mode: "number" })
      .notNull()
      .references(() => departments.id),
    name: varchar("name", { length: 255 }).notNull(),
    description: text("description"),
    ageRange: varchar("age_range", { length: 191 }),
    room: varchar("room", { length: 191 }),
    status: varchar("status", { length: 80 }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("ebd_classes_id_tenant_unique").on(table.id, table.tenantId),
    uniqueIndex("ebd_classes_tenant_department_name_unique").on(
      table.tenantId,
      table.departmentId,
      table.name,
    ),
  ],
);
export const ebdClassTeachers = mysqlTable(
  "ebd_class_teachers",
  {
    classId: bigint("class_id", { mode: "number" })
      .notNull()
      .references(() => ebdClasses.id),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    membershipId: bigint("membership_id", { mode: "number" })
      .notNull()
      .references(() => tenantMemberships.id),
    personId: bigint("person_id", { mode: "number" }).references(() => people.id),
    teacherRole: varchar("teacher_role", { length: 191 }).notNull(),
    status: varchar("status", { length: 80 }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.classId, table.membershipId] }),
    index("ebd_class_teachers_membership_idx").on(
      table.tenantId,
      table.membershipId,
      table.status,
    ),
  ],
);
export const ebdEnrollments = mysqlTable(
  "ebd_enrollments",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    departmentId: bigint("department_id", { mode: "number" })
      .notNull()
      .references(() => departments.id),
    classId: bigint("class_id", { mode: "number" })
      .notNull()
      .references(() => ebdClasses.id),
    personId: bigint("person_id", { mode: "number" })
      .notNull()
      .references(() => people.id),
    enrolledAt: varchar("enrolled_at", { length: 40 }).notNull(),
    status: varchar("status", { length: 80 }).notNull(),
    leftAt: varchar("left_at", { length: 40 }),
    notes: text("notes"),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("ebd_enrollments_class_status_idx").on(
      table.tenantId,
      table.classId,
      table.status,
      table.personId,
    ),
  ],
);
export const ebdStudents = mysqlTable(
  "ebd_students",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    departmentId: bigint("department_id", { mode: "number" })
      .notNull()
      .references(() => departments.id),
    personId: bigint("person_id", { mode: "number" }).references(() => people.id),
    fullName: varchar("full_name", { length: 255 }).notNull(),
    birthDate: varchar("birth_date", { length: 40 }),
    sex: varchar("sex", { length: 191 }),
    cpf: varchar("cpf", { length: 32 }),
    phone: varchar("phone", { length: 40 }),
    whatsapp: varchar("whatsapp", { length: 40 }),
    guardianName: varchar("guardian_name", { length: 255 }),
    guardianPhone: varchar("guardian_phone", { length: 40 }),
    notes: text("notes"),
    status: varchar("status", { length: 80 }).notNull(),
    createdByUserId: bigint("created_by_user_id", { mode: "number" }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("ebd_students_department_name_idx").on(
      table.tenantId,
      table.departmentId,
      table.status,
      table.fullName,
    ),
  ],
);
export const ebdStudentEnrollments = mysqlTable(
  "ebd_student_enrollments",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    departmentId: bigint("department_id", { mode: "number" })
      .notNull()
      .references(() => departments.id),
    classId: bigint("class_id", { mode: "number" })
      .notNull()
      .references(() => ebdClasses.id),
    studentId: bigint("student_id", { mode: "number" })
      .notNull()
      .references(() => ebdStudents.id),
    enrolledAt: varchar("enrolled_at", { length: 40 }).notNull(),
    status: varchar("status", { length: 80 }).notNull(),
    leftAt: varchar("left_at", { length: 40 }),
    notes: text("notes"),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("ebd_student_enrollments_class_idx").on(
      table.tenantId,
      table.classId,
      table.status,
      table.studentId,
    ),
  ],
);
export const secretaryRequests = mysqlTable(
  "secretary_requests",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    personId: bigint("person_id", { mode: "number" })
      .notNull()
      .references(() => people.id),
    requestType: varchar("request_type", { length: 80 }).notNull(),
    requestDirection: varchar("request_direction", { length: 191 }).notNull().default("SAIDA"),
    originUnitId: bigint("origin_unit_id", { mode: "number" }),
    destinationUnitId: bigint("destination_unit_id", { mode: "number" }),
    externalChurch: varchar("external_church", { length: 191 }),
    externalCity: varchar("external_city", { length: 191 }),
    externalState: varchar("external_state", { length: 191 }),
    reason: varchar("reason", { length: 191 }),
    notes: text("notes"),
    status: varchar("status", { length: 80 }).notNull(),
    departmentResolution: text("department_resolution"),
    ebdResolution: text("ebd_resolution"),
    version: bigint("version", { mode: "number" }).notNull(),
    requestedByUserId: bigint("requested_by_user_id", { mode: "number" }).notNull(),
    reviewedByUserId: bigint("reviewed_by_user_id", { mode: "number" }),
    requestedAt: varchar("requested_at", { length: 40 }).notNull(),
    reviewedAt: varchar("reviewed_at", { length: 40 }),
    completedAt: varchar("completed_at", { length: 40 }),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("secretary_requests_queue_idx").on(
      table.tenantId,
      table.status,
      table.requestType,
      table.requestedAt,
    ),
    index("secretary_requests_pending_destination_idx").on(
      table.tenantId,
      table.personId,
      table.destinationUnitId,
      table.status,
    ),
  ],
);
export const secretaryTransferSearchLimits = mysqlTable(
  "secretary_transfer_search_limits",
  {
    tenantId: bigint("tenant_id", { mode: "number" }).notNull().references(() => tenants.id),
    userId: bigint("user_id", { mode: "number" }).notNull().references(() => authUsers.id),
    attempts: bigint("attempts", { mode: "number" }).notNull().default(0),
    windowStartedAt: varchar("window_started_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.tenantId, table.userId] }),
    index("secretary_transfer_search_limits_window_idx").on(table.windowStartedAt, table.updatedAt),
  ],
);
export const churchMovements = mysqlTable(
  "church_movements",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    personId: bigint("person_id", { mode: "number" })
      .notNull()
      .references(() => people.id),
    unitId: bigint("unit_id", { mode: "number" }).notNull(),
    movementType: varchar("movement_type", { length: 80 }).notNull(),
    requestId: bigint("request_id", { mode: "number" }),
    effectiveDate: varchar("effective_date", { length: 40 }).notNull(),
    previousStatus: varchar("previous_status", { length: 80 }),
    newStatus: varchar("new_status", { length: 80 }),
    previousUnitId: bigint("previous_unit_id", { mode: "number" }),
    destinationUnitId: bigint("destination_unit_id", { mode: "number" }),
    externalChurch: varchar("external_church", { length: 191 }),
    externalCity: varchar("external_city", { length: 191 }),
    externalState: varchar("external_state", { length: 191 }),
    description: text("description").notNull(),
    metadataJson: text("metadata_json").notNull(),
    status: varchar("status", { length: 80 }).notNull(),
    createdByUserId: bigint("created_by_user_id", { mode: "number" }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("church_movements_report_idx").on(
      table.tenantId,
      table.unitId,
      table.movementType,
      table.effectiveDate,
      table.status,
    ),
  ],
);
export const baptismEvents = mysqlTable(
  "baptism_events",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    unitId: bigint("unit_id", { mode: "number" }).notNull(),
    title: varchar("title", { length: 255 }).notNull(),
    scheduledDate: varchar("scheduled_date", { length: 40 }).notNull(),
    location: varchar("location", { length: 191 }),
    responsiblePersonId: bigint("responsible_person_id", { mode: "number" }),
    notes: text("notes"),
    status: varchar("status", { length: 80 }).notNull(),
    version: bigint("version", { mode: "number" }).notNull(),
    createdByUserId: bigint("created_by_user_id", { mode: "number" }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("baptism_events_date_idx").on(
      table.tenantId,
      table.unitId,
      table.scheduledDate,
      table.status,
    ),
  ],
);
export const baptismCandidates = mysqlTable(
  "baptism_candidates",
  {
    eventId: bigint("event_id", { mode: "number" })
      .notNull()
      .references(() => baptismEvents.id),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    personId: bigint("person_id", { mode: "number" })
      .notNull()
      .references(() => people.id),
    status: varchar("status", { length: 80 }).notNull(),
    notes: text("notes"),
    completedAt: varchar("completed_at", { length: 40 }),
    updatedByUserId: bigint("updated_by_user_id", { mode: "number" }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.eventId, table.personId] }),
    index("baptism_candidates_status_idx").on(
      table.tenantId,
      table.status,
      table.personId,
    ),
  ],
);
export const consecrations = mysqlTable(
  "consecrations",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    personId: bigint("person_id", { mode: "number" })
      .notNull()
      .references(() => people.id),
    unitId: bigint("unit_id", { mode: "number" }).notNull(),
    previousFunctionId: bigint("previous_function_id", { mode: "number" }),
    newFunctionId: bigint("new_function_id", { mode: "number" }).notNull(),
    eventDate: varchar("event_date", { length: 40 }).notNull(),
    location: varchar("location", { length: 191 }),
    responsiblePersonId: bigint("responsible_person_id", { mode: "number" }),
    notes: text("notes"),
    status: varchar("status", { length: 80 }).notNull(),
    version: bigint("version", { mode: "number" }).notNull(),
    createdByUserId: bigint("created_by_user_id", { mode: "number" }).notNull(),
    completedByUserId: bigint("completed_by_user_id", { mode: "number" }),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("consecrations_status_date_idx").on(
      table.tenantId,
      table.unitId,
      table.status,
      table.eventDate,
    ),
  ],
);
export const secretaryDocumentTemplates = mysqlTable(
  "secretary_document_templates",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    unitId: bigint("unit_id", { mode: "number" }),
    name: varchar("name", { length: 255 }).notNull(),
    documentType: varchar("document_type", { length: 80 }).notNull(),
    status: varchar("status", { length: 80 }).notNull(),
    currentVersion: bigint("current_version", { mode: "number" }).notNull(),
    createdByUserId: bigint("created_by_user_id", { mode: "number" }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
);
export const secretaryDocumentTemplateVersions = mysqlTable(
  "secretary_document_template_versions",
  {
    templateId: bigint("template_id", { mode: "number" })
      .notNull()
      .references(() => secretaryDocumentTemplates.id),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    version: bigint("version", { mode: "number" }).notNull(),
    title: varchar("title", { length: 255 }).notNull(),
    body: text("body").notNull(),
    headerText: text("header_text"),
    footerText: text("footer_text"),
    signatureLabelsJson: text("signature_labels_json").notNull(),
    styleJson: text("style_json").notNull(),
    createdByUserId: bigint("created_by_user_id", { mode: "number" }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
  (table) => [primaryKey({ columns: [table.templateId, table.version] })],
);
export const secretaryDocuments = mysqlTable(
  "secretary_documents",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    unitId: bigint("unit_id", { mode: "number" }).notNull(),
    personId: bigint("person_id", { mode: "number" })
      .notNull()
      .references(() => people.id),
    templateId: bigint("template_id", { mode: "number" })
      .notNull()
      .references(() => secretaryDocumentTemplates.id),
    templateVersion: bigint("template_version", { mode: "number" }).notNull(),
    documentType: varchar("document_type", { length: 80 }).notNull(),
    documentNumber: varchar("document_number", { length: 191 }).notNull(),
    titleSnapshot: varchar("title_snapshot", { length: 191 }).notNull(),
    bodySnapshot: text("body_snapshot").notNull(),
    headerSnapshot: text("header_snapshot"),
    footerSnapshot: text("footer_snapshot"),
    signaturesSnapshot: text("signatures_snapshot").notNull(),
    issuedByUserId: bigint("issued_by_user_id", { mode: "number" }).notNull(),
    issuedAt: varchar("issued_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("secretary_documents_report_idx").on(
      table.tenantId,
      table.unitId,
      table.documentType,
      table.issuedAt,
    ),
  ],
);
export const ebdMeetings = mysqlTable(
  "ebd_meetings",
  {
    id: bigint("id", { mode: "number" }).primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    departmentId: bigint("department_id", { mode: "number" })
      .notNull()
      .references(() => departments.id),
    meetingDate: varchar("meeting_date", { length: 40 }).notNull(),
    theme: varchar("theme", { length: 191 }),
    startTime: varchar("start_time", { length: 40 }),
    status: varchar("status", { length: 80 }).notNull(),
    version: bigint("version", { mode: "number" }).notNull(),
    createdByUserId: bigint("created_by_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    finalizedByUserId: bigint("finalized_by_user_id", { mode: "number" }).references(
      () => authUsers.id,
    ),
    finalizedAt: varchar("finalized_at", { length: 40 }),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    uniqueIndex("ebd_meetings_tenant_department_date_unique").on(
      table.tenantId,
      table.departmentId,
      table.meetingDate,
    ),
  ],
);
export const ebdStudentAttendance = mysqlTable(
  "ebd_student_attendance",
  {
    meetingId: bigint("meeting_id", { mode: "number" })
      .notNull()
      .references(() => ebdMeetings.id),
    classId: bigint("class_id", { mode: "number" })
      .notNull()
      .references(() => ebdClasses.id),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    studentId: bigint("student_id", { mode: "number" })
      .notNull()
      .references(() => ebdStudents.id),
    attendanceStatus: varchar("attendance_status", { length: 80 }).notNull(),
    updatedByUserId: bigint("updated_by_user_id", { mode: "number" }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.meetingId, table.classId, table.studentId] }),
    index("ebd_student_attendance_history_idx").on(
      table.tenantId,
      table.studentId,
      table.attendanceStatus,
      table.meetingId,
    ),
  ],
);
export const ebdAttendance = mysqlTable(
  "ebd_attendance",
  {
    meetingId: bigint("meeting_id", { mode: "number" })
      .notNull()
      .references(() => ebdMeetings.id),
    classId: bigint("class_id", { mode: "number" })
      .notNull()
      .references(() => ebdClasses.id),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    personId: bigint("person_id", { mode: "number" })
      .notNull()
      .references(() => people.id),
    attendanceStatus: varchar("attendance_status", { length: 80 }).notNull(),
    updatedByUserId: bigint("updated_by_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.meetingId, table.classId, table.personId] }),
    index("ebd_attendance_person_history_idx").on(
      table.tenantId,
      table.personId,
      table.attendanceStatus,
      table.meetingId,
    ),
  ],
);
export const ebdVisitors = mysqlTable("ebd_visitors", {
  id: bigint("id", { mode: "number" }).primaryKey(),
  tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
  departmentId: bigint("department_id", { mode: "number" })
    .notNull()
    .references(() => departments.id),
  meetingId: bigint("meeting_id", { mode: "number" })
    .notNull()
    .references(() => ebdMeetings.id),
  classId: bigint("class_id", { mode: "number" })
    .notNull()
    .references(() => ebdClasses.id),
  personId: bigint("person_id", { mode: "number" }).references(() => people.id),
  name: varchar("name", { length: 255 }).notNull(),
  phone: varchar("phone", { length: 40 }),
  ageRange: varchar("age_range", { length: 191 }),
  invitedBy: varchar("invited_by", { length: 191 }),
  notes: text("notes"),
  createdByUserId: bigint("created_by_user_id", { mode: "number" })
    .notNull()
    .references(() => authUsers.id),
  createdAt: varchar("created_at", { length: 40 }).notNull(),
});
export const ebdClassSummaries = mysqlTable(
  "ebd_class_summaries",
  {
    meetingId: bigint("meeting_id", { mode: "number" })
      .notNull()
      .references(() => ebdMeetings.id),
    classId: bigint("class_id", { mode: "number" })
      .notNull()
      .references(() => ebdClasses.id),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    enrolledCount: bigint("enrolled_count", { mode: "number" }).notNull(),
    presentCount: bigint("present_count", { mode: "number" }).notNull(),
    absentCount: bigint("absent_count", { mode: "number" }).notNull(),
    justifiedCount: bigint("justified_count", { mode: "number" }).notNull(),
    visitorCount: bigint("visitor_count", { mode: "number" }).notNull(),
    bibleCount: bigint("bible_count", { mode: "number" }).notNull(),
    assistanceCount: bigint("assistance_count", { mode: "number" }).notNull(),
    offeringCents: bigint("offering_cents", { mode: "number" }).notNull(),
    notes: text("notes"),
    status: varchar("status", { length: 80 }).notNull(),
    version: bigint("version", { mode: "number" }).notNull(),
    finalizedByUserId: bigint("finalized_by_user_id", { mode: "number" }).references(
      () => authUsers.id,
    ),
    finalizedAt: varchar("finalized_at", { length: 40 }),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.meetingId, table.classId] }),
    index("ebd_class_summaries_status_idx").on(
      table.tenantId,
      table.meetingId,
      table.status,
    ),
  ],
);
export const ebdClosures = mysqlTable("ebd_closures", {
  meetingId: bigint("meeting_id", { mode: "number" })
    .primaryKey()
    .references(() => ebdMeetings.id),
  tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
  departmentId: bigint("department_id", { mode: "number" })
    .notNull()
    .references(() => departments.id),
  enrolledTotal: bigint("enrolled_total", { mode: "number" }).notNull(),
  presentTotal: bigint("present_total", { mode: "number" }).notNull(),
  absentTotal: bigint("absent_total", { mode: "number" }).notNull(),
  justifiedTotal: bigint("justified_total", { mode: "number" }).notNull(),
  visitorTotal: bigint("visitor_total", { mode: "number" }).notNull(),
  bibleTotal: bigint("bible_total", { mode: "number" }).notNull(),
  assistanceTotal: bigint("assistance_total", { mode: "number" }).notNull(),
  offeringTotalCents: bigint("offering_total_cents", { mode: "number" }).notNull(),
  exceptionReason: varchar("exception_reason", { length: 191 }),
  finalizedByUserId: bigint("finalized_by_user_id", { mode: "number" })
    .notNull()
    .references(() => authUsers.id),
  finalizedAt: varchar("finalized_at", { length: 40 }).notNull(),
  updatedAt: varchar("updated_at", { length: 40 }).notNull(),
});
export const departmentAudit = mysqlTable(
  "department_audit",
  {
    id: bigint("id", { mode: "number" }).autoincrement().primaryKey(),
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    departmentId: bigint("department_id", { mode: "number" })
      .notNull()
      .references(() => departments.id),
    actorUserId: bigint("actor_user_id", { mode: "number" })
      .notNull()
      .references(() => authUsers.id),
    actorMembershipId: bigint("actor_membership_id", { mode: "number" }).references(
      () => tenantMemberships.id,
    ),
    action: varchar("action", { length: 191 }).notNull(),
    entityType: varchar("entity_type", { length: 80 }).notNull(),
    entityId: bigint("entity_id", { mode: "number" }).notNull(),
    previousValues: text("previous_values"),
    newValues: text("new_values"),
    reason: varchar("reason", { length: 191 }),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("department_audit_department_created_idx").on(
      table.tenantId,
      table.departmentId,
      table.createdAt,
    ),
  ],
);

export const financeAllocationConfigs = mysqlTable(
  "finance_allocation_configs",
  {
    id: bigint("id", { mode: "number" }).primaryKey(), tenantId: bigint("tenant_id", { mode: "number" }).notNull(), unitId: bigint("unit_id", { mode: "number" }).notNull(),
    version: bigint("version", { mode: "number" }).notNull().default(1), status: varchar("status", { length: 80 }).notNull().default("ATIVA"),
    createdByUserId: bigint("created_by_user_id", { mode: "number" }).notNull(), updatedByUserId: bigint("updated_by_user_id", { mode: "number" }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(), updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [uniqueIndex("finance_allocation_configs_unit_unique").on(table.tenantId,table.unitId),index("finance_allocation_configs_scope_idx").on(table.tenantId,table.unitId,table.status)],
);

export const financeAllocationRules = mysqlTable(
  "finance_allocation_rules",
  {
    id: bigint("id", { mode: "number" }).primaryKey(), tenantId: bigint("tenant_id", { mode: "number" }).notNull(), configId: bigint("config_id", { mode: "number" }).notNull(),
    recipientName: varchar("recipient_name", { length: 255 }).notNull(), description: text("description"), recipientType: varchar("recipient_type", { length: 80 }).notNull(),
    ruleType: varchar("rule_type", { length: 80 }).notNull().default("PERCENTUAL"), percentageBasisPoints: bigint("percentage_basis_points", { mode: "number" }), fixedAmountCents: bigint("fixed_amount_cents", { mode: "number" }), financialDestination: varchar("financial_destination", { length: 191 }).notNull().default("REPASSAR"), destinationUnitId: bigint("destination_unit_id", { mode: "number" }), destinationDepartmentId: bigint("destination_department_id", { mode: "number" }),
    calculationBase: varchar("calculation_base", { length: 191 }).notNull().default("RECEITAS_PARTICIPANTES"), displayOrder: bigint("display_order", { mode: "number" }).notNull(), active: bigint("active", { mode: "number" }).notNull().default(1),
    createdAt: varchar("created_at", { length: 40 }).notNull(), updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [index("finance_allocation_rules_config_idx").on(table.tenantId,table.configId,table.active,table.displayOrder)],
);

export const financePeriods = mysqlTable(
  "finance_periods",
  {
    id: bigint("id", { mode: "number" }).primaryKey(), tenantId: bigint("tenant_id", { mode: "number" }).notNull(), unitId: bigint("unit_id", { mode: "number" }).notNull(), unitType: varchar("unit_type", { length: 80 }).notNull(),
    matrixId: bigint("matrix_id", { mode: "number" }).notNull(), branchId: bigint("branch_id", { mode: "number" }), competency: varchar("competency", { length: 16 }).notNull(), status: varchar("status", { length: 80 }).notNull().default("ABERTO"),
    lifecycleState: varchar("lifecycle_state", { length: 191 }).notNull().default("ABERTO"), allocationConfigId: bigint("allocation_config_id", { mode: "number" }).notNull(), allocationConfigVersion: bigint("allocation_config_version", { mode: "number" }).notNull(),
    openedAt: varchar("opened_at", { length: 40 }).notNull(), openedByUserId: bigint("opened_by_user_id", { mode: "number" }).notNull(), closedAt: varchar("closed_at", { length: 40 }), closedByUserId: bigint("closed_by_user_id", { mode: "number" }),
    reopenedAt: varchar("reopened_at", { length: 40 }), reopenedByUserId: bigint("reopened_by_user_id", { mode: "number" }), reopenReason: varchar("reopen_reason", { length: 191 }), reopenCount: bigint("reopen_count", { mode: "number" }).notNull().default(0),
    closureVersion: bigint("closure_version", { mode: "number" }).notNull().default(0), notes: text("notes"), version: bigint("version", { mode: "number" }).notNull().default(1), createdAt: varchar("created_at", { length: 40 }).notNull(), updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [uniqueIndex("finance_periods_unit_competency_unique").on(table.tenantId,table.unitId,table.competency),index("finance_periods_scope_competency_idx").on(table.tenantId,table.matrixId,table.branchId,table.competency,table.status)],
);

export const financePeriodReopenRequests = mysqlTable(
  "finance_period_reopen_requests",
  {
    id: bigint("id", { mode: "number" }).primaryKey(), tenantId: bigint("tenant_id", { mode: "number" }).notNull(), periodId: bigint("period_id", { mode: "number" }).notNull(), unitId: bigint("unit_id", { mode: "number" }).notNull(), matrixId: bigint("matrix_id", { mode: "number" }).notNull(), branchId: bigint("branch_id", { mode: "number" }),
    requesterUserId: bigint("requester_user_id", { mode: "number" }).notNull(), requesterMembershipId: bigint("requester_membership_id", { mode: "number" }).notNull(), requestedClosureVersion: bigint("requested_closure_version", { mode: "number" }).notNull(), reason: varchar("reason", { length: 191 }).notNull(), status: varchar("status", { length: 80 }).notNull().default("PENDENTE"),
    requestedAt: varchar("requested_at", { length: 40 }).notNull(), expiresAt: varchar("expires_at", { length: 40 }), decidedByUserId: bigint("decided_by_user_id", { mode: "number" }), decidedByMembershipId: bigint("decided_by_membership_id", { mode: "number" }), decisionReason: varchar("decision_reason", { length: 191 }), decidedAt: varchar("decided_at", { length: 40 }), usedAt: varchar("used_at", { length: 40 }), reopenedByUserId: bigint("reopened_by_user_id", { mode: "number" }), createdAt: varchar("created_at", { length: 40 }).notNull(), updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    index("finance_reopen_requests_matrix_status_idx").on(table.tenantId, table.matrixId, table.status, table.requestedAt),
    index("finance_reopen_requests_requester_idx").on(table.tenantId, table.requesterUserId, table.periodId, table.requestedClosureVersion),
  ],
);

export const financePeriodAllocationRules = mysqlTable("finance_period_allocation_rules", {
  id: bigint("id", { mode: "number" }).primaryKey(), tenantId: bigint("tenant_id", { mode: "number" }).notNull(), periodId: bigint("period_id", { mode: "number" }).notNull(), sourceRuleId: bigint("source_rule_id", { mode: "number" }).notNull(),
  recipientName: varchar("recipient_name", { length: 255 }).notNull(), description: text("description"), recipientType: varchar("recipient_type", { length: 80 }).notNull(), ruleType: varchar("rule_type", { length: 80 }).notNull(),
  percentageBasisPoints: bigint("percentage_basis_points", { mode: "number" }), fixedAmountCents: bigint("fixed_amount_cents", { mode: "number" }), financialDestination: varchar("financial_destination", { length: 191 }).notNull().default("REPASSAR"), destinationUnitId: bigint("destination_unit_id", { mode: "number" }), destinationDepartmentId: bigint("destination_department_id", { mode: "number" }), calculationBase: varchar("calculation_base", { length: 191 }).notNull(),
  participatingCategoryIdsJson: text("participating_category_ids_json").notNull().default("[]"), displayOrder: bigint("display_order", { mode: "number" }).notNull(), snapshotVersion: bigint("snapshot_version", { mode: "number" }).notNull(), createdAt: varchar("created_at", { length: 40 }).notNull(),
});

export const financeInterunitRepasses = mysqlTable("finance_interunit_repasses", {
  id: bigint("id", { mode: "number" }).primaryKey(), tenantId: bigint("tenant_id", { mode: "number" }).notNull(), periodId: bigint("period_id", { mode: "number" }).notNull(), closureVersion: bigint("closure_version", { mode: "number" }).notNull(), ruleDisplayOrder: bigint("rule_display_order", { mode: "number" }).notNull(), sourceRuleId: bigint("source_rule_id", { mode: "number" }), sourceUnitId: bigint("source_unit_id", { mode: "number" }).notNull(), destinationUnitId: bigint("destination_unit_id", { mode: "number" }).notNull(), destinationDepartmentId: bigint("destination_department_id", { mode: "number" }), kind: varchar("kind", { length: 80 }).notNull().default("NORMAL"), payerUnitId: bigint("payer_unit_id", { mode: "number" }).notNull(), receiverUnitId: bigint("receiver_unit_id", { mode: "number" }).notNull(), recipientName: varchar("recipient_name", { length: 255 }).notNull(), competency: varchar("competency", { length: 16 }).notNull(), expectedCents: bigint("expected_cents", { mode: "number" }).notNull(), sentCents: bigint("sent_cents", { mode: "number" }).notNull().default(0), receivedCents: bigint("received_cents", { mode: "number" }).notNull().default(0), writtenOffCents: bigint("written_off_cents", { mode: "number" }).notNull().default(0), status: varchar("status", { length: 80 }).notNull().default("PENDENTE"), supersededById: bigint("superseded_by_id", { mode: "number" }), createdByUserId: bigint("created_by_user_id", { mode: "number" }).notNull(), createdAt: varchar("created_at", { length: 40 }).notNull(), updatedAt: varchar("updated_at", { length: 40 }).notNull(),
}, (table) => [uniqueIndex("finance_interunit_repasses_version_unique").on(table.tenantId,table.periodId,table.closureVersion,table.ruleDisplayOrder,table.kind,table.payerUnitId,table.receiverUnitId),index("finance_interunit_repasses_scope_idx").on(table.tenantId,table.payerUnitId,table.receiverUnitId,table.status,table.createdAt)]);

export const financeInterunitRepassEvents = mysqlTable("finance_interunit_repass_events", {
  id: bigint("id", { mode: "number" }).primaryKey(), tenantId: bigint("tenant_id", { mode: "number" }).notNull(), repassId: bigint("repass_id", { mode: "number" }).notNull(), eventType: varchar("event_type", { length: 80 }).notNull(), amountCents: bigint("amount_cents", { mode: "number" }).notNull(), accountId: bigint("account_id", { mode: "number" }), movementId: bigint("movement_id", { mode: "number" }), occurredOn: varchar("occurred_on", { length: 40 }).notNull(), reason: varchar("reason", { length: 191 }), actorUserId: bigint("actor_user_id", { mode: "number" }).notNull(), createdAt: varchar("created_at", { length: 40 }).notNull(),
}, (table) => [index("finance_interunit_repass_events_idx").on(table.tenantId,table.repassId,table.createdAt),uniqueIndex("finance_interunit_repass_events_movement_unique").on(table.tenantId,table.movementId)]);

export const financeQuickSessions = mysqlTable(
  "finance_quick_sessions",
  {
    id: bigint("id", { mode: "number" }).primaryKey(), tenantId: bigint("tenant_id", { mode: "number" }).notNull(), periodId: bigint("period_id", { mode: "number" }).notNull(), unitId: bigint("unit_id", { mode: "number" }).notNull(), userId: bigint("user_id", { mode: "number" }).notNull(),
    accountId: bigint("account_id", { mode: "number" }).notNull(), defaultDate: varchar("default_date", { length: 40 }).notNull(), defaultCompetency: varchar("default_competency", { length: 191 }).notNull(), defaultContributionType: varchar("default_contribution_type", { length: 80 }).notNull(),
    defaultPaymentMethodId: bigint("default_payment_method_id", { mode: "number" }), status: varchar("status", { length: 80 }).notNull().default("EM_ANDAMENTO"), entryCount: bigint("entry_count", { mode: "number" }).notNull().default(0), totalCents: bigint("total_cents", { mode: "number" }).notNull().default(0),
    startedAt: varchar("started_at", { length: 40 }).notNull(), finishedAt: varchar("finished_at", { length: 40 }), updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [index("finance_quick_sessions_resume_idx").on(table.tenantId,table.userId,table.periodId,table.status,table.updatedAt)],
);

export const financeContributionCategoryDefaults = mysqlTable(
  "finance_contribution_category_defaults",
  {
    tenantId: bigint("tenant_id", { mode: "number" }).notNull(),
    contributionType: varchar("contribution_type", { length: 80 }).notNull(),
    categoryId: bigint("category_id", { mode: "number" }).notNull(),
    createdByUserId: bigint("created_by_user_id", { mode: "number" }).notNull(),
    updatedByUserId: bigint("updated_by_user_id", { mode: "number" }).notNull(),
    createdAt: varchar("created_at", { length: 40 }).notNull(),
    updatedAt: varchar("updated_at", { length: 40 }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.tenantId, table.contributionType] }),
    index("finance_contribution_category_defaults_category_idx").on(
      table.tenantId,
      table.categoryId,
    ),
  ],
);
